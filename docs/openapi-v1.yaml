openapi: 3.1.0
info:
  title: Cheesy Arena API
  version: 1.0.0-draft
  description: >-
    Versioned API introduced alongside the existing server-rendered interface.
    This document is expanded as each migration phase is implemented.
servers:
  - url: /api/v1
paths:
  /alliances:
    get:
      operationId: listAlliances
      summary: List playoff alliances
      responses:
        "200":
          description: Alliances ordered by ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllianceListResponse"
  /bracket:
    get:
      operationId: getBracket
      summary: Get the playoff bracket as structured data
      parameters:
        - in: query
          name: activeMatch
          schema:
            type: string
            enum: [current, saved]
      responses:
        "200":
          description: Playoff bracket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BracketResponse"
  /event:
    get:
      operationId: getPublicEvent
      summary: Get public event configuration
      responses:
        "200":
          description: Public event configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicEventResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /session:
    get:
      operationId: getSession
      summary: Get the current session state and CSRF token
      responses:
        "200":
          description: Session state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
  /game/rules:
    get:
      operationId: listGameRules
      summary: List foul rules ordered by ID
      responses:
        "200":
          description: Game rules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RuleListResponse"
  /match-logs:
    get:
      operationId: listMatchLogs
      summary: List matches available to the match log interface
      responses:
        "200":
          description: Matches grouped by type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchLogListResponse"
  /matches/{type}:
    get:
      operationId: listMatches
      summary: List matches and their latest results
      parameters:
        - in: path
          name: type
          required: true
          schema:
            $ref: "#/components/schemas/MatchType"
      responses:
        "200":
          description: Matches ordered by type order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchListResponse"
        "400":
          description: Invalid match type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /matches/{matchId}/stations/{stationId}/logs:
    get:
      operationId: getMatchStationLogs
      summary: Get bounded and optionally sampled driver station log rows
      parameters:
        - in: path
          name: matchId
          required: true
          schema:
            type: integer
        - in: path
          name: stationId
          required: true
          schema:
            type: string
            enum: [R1, R2, R3, B1, B2, B3]
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
            default: 0
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 10000
            default: 1000
        - in: query
          name: sampleEvery
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
      responses:
        "200":
          description: Match log details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchLogDetailResponse"
        "400":
          description: Invalid query parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Match or station log not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /rankings:
    get:
      operationId: listRankings
      summary: List qualification rankings
      responses:
        "200":
          description: Rankings and highest played match
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RankingsResponse"
  /sponsor-slides:
    get:
      operationId: listSponsorSlides
      summary: List public sponsor slides
      responses:
        "200":
          description: Sponsor slides in display order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SponsorSlideListResponse"
  /teams:
    get:
      operationId: listPublicTeams
      summary: List teams without private network or FTA fields
      responses:
        "200":
          description: Public teams ordered by team number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamListResponse"
  /teams/{teamId}/avatar:
    get:
      operationId: getTeamAvatar
      summary: Get a team avatar or the default avatar
      parameters:
        - in: path
          name: teamId
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Avatar image
          content:
            image/png:
              schema:
                type: string
                contentEncoding: binary
  /admin/awards:
    get:
      operationId: listAdminAwards
      summary: List awards for administration
      security: [{sessionCookie: []}]
      responses:
        "200": {$ref: "#/components/responses/AdminAwardList"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      operationId: createAdminAward
      summary: Create a judged award
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminAwardInput"}
      responses:
        "201": {$ref: "#/components/responses/AdminAwardItem"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/awards/{id}:
    parameters: [{$ref: "#/components/parameters/ResourceId"}]
    patch:
      operationId: updateAdminAward
      summary: Update a judged award
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminAwardInput"}
      responses:
        "200": {$ref: "#/components/responses/AdminAwardItem"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/ValidationError"}
    delete:
      operationId: deleteAdminAward
      summary: Delete a judged award
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "204": {description: Award deleted}
        "404": {$ref: "#/components/responses/NotFound"}
  /admin/scheduled-breaks:
    get:
      operationId: listAdminScheduledBreaks
      summary: List scheduled breaks for administration
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Scheduled breaks
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminScheduledBreakListResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
  /admin/scheduled-breaks/{id}:
    parameters: [{$ref: "#/components/parameters/ResourceId"}]
    patch:
      operationId: updateAdminScheduledBreak
      summary: Update the description of a scheduled break
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminScheduledBreakInput"}
      responses:
        "200":
          description: Updated scheduled break
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminScheduledBreakResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
  /admin/sponsor-slides:
    get:
      operationId: listAdminSponsorSlides
      summary: List sponsor slides for administration
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Sponsor slides
          content:
            application/json:
              schema: {$ref: "#/components/schemas/SponsorSlideListResponse"}
    post:
      operationId: createAdminSponsorSlide
      summary: Create a sponsor slide
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminSponsorSlideInput"}
      responses:
        "201":
          description: Created sponsor slide
          content:
            application/json:
              schema: {$ref: "#/components/schemas/SponsorSlideResponse"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/sponsor-slides/{id}:
    parameters: [{$ref: "#/components/parameters/ResourceId"}]
    patch:
      operationId: updateAdminSponsorSlide
      summary: Update a sponsor slide
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminSponsorSlideInput"}
      responses:
        "200":
          description: Updated sponsor slide
          content:
            application/json:
              schema: {$ref: "#/components/schemas/SponsorSlideResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/ValidationError"}
    delete:
      operationId: deleteAdminSponsorSlide
      summary: Delete a sponsor slide
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "204": {description: Sponsor slide deleted}
        "404": {$ref: "#/components/responses/NotFound"}
  /admin/sponsor-slides/reorder:
    post:
      operationId: reorderAdminSponsorSlides
      summary: Replace the display order of all sponsor slides
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminReorderInput"}
      responses:
        "200":
          description: Reordered sponsor slides
          content:
            application/json:
              schema: {$ref: "#/components/schemas/SponsorSlideListResponse"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/lower-thirds:
    get:
      operationId: listAdminLowerThirds
      summary: List lower thirds for administration
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Lower thirds
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminLowerThirdListResponse"}
    post:
      operationId: createAdminLowerThird
      summary: Create a lower third
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminLowerThirdInput"}
      responses:
        "201": {$ref: "#/components/responses/AdminLowerThirdItem"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/lower-thirds/{id}:
    parameters: [{$ref: "#/components/parameters/ResourceId"}]
    patch:
      operationId: updateAdminLowerThird
      summary: Update a lower third
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminLowerThirdInput"}
      responses:
        "200": {$ref: "#/components/responses/AdminLowerThirdItem"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/ValidationError"}
    delete:
      operationId: deleteAdminLowerThird
      summary: Delete a lower third
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "204": {description: Lower third deleted}
        "404": {$ref: "#/components/responses/NotFound"}
  /admin/lower-thirds/reorder:
    post:
      operationId: reorderAdminLowerThirds
      summary: Replace the display order of all lower thirds
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminReorderInput"}
      responses:
        "200":
          description: Reordered lower thirds
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminLowerThirdListResponse"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/teams:
    get:
      operationId: listAdminTeams
      summary: List complete administrative team records with secrets masked
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Teams and team-list state
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminTeamListResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      operationId: importAdminTeams
      summary: Start a bulk team import job
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminTeamImportInput"}
      responses:
        "202": {$ref: "#/components/responses/AdminJobItem"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/teams/{id}:
    parameters: [{$ref: "#/components/parameters/ResourceId"}]
    get:
      operationId: getAdminTeam
      summary: Get an administrative team record with its WPA key masked
      security: [{sessionCookie: []}]
      responses:
        "200": {$ref: "#/components/responses/AdminTeamItem"}
        "404": {$ref: "#/components/responses/NotFound"}
    patch:
      operationId: updateAdminTeam
      summary: Update team metadata with explicit WPA key secret semantics
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminTeamUpdateInput"}
      responses:
        "200": {$ref: "#/components/responses/AdminTeamItem"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/ValidationError"}
    delete:
      operationId: deleteAdminTeam
      summary: Delete a team before qualification matches exist
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "204": {description: Team deleted}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
  /admin/teams/refresh:
    post:
      operationId: refreshAdminTeams
      summary: Start a job to refresh every team from The Blue Alliance
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "202": {$ref: "#/components/responses/AdminJobItem"}
  /admin/teams/wpa-keys:
    post:
      operationId: generateAdminTeamWpaKeys
      summary: Generate missing or replacement WPA keys without returning them
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminTeamWpaKeysInput"}
      responses:
        "200":
          description: Number of teams updated
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminTeamWpaKeysResponse"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/jobs/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: {type: string, format: uuid}
    get:
      operationId: getAdminJob
      summary: Get background job progress and outcome
      security: [{sessionCookie: []}]
      responses:
        "200": {$ref: "#/components/responses/AdminJobItem"}
        "404": {$ref: "#/components/responses/NotFound"}
  /admin/judging-schedule:
    get:
      operationId: getAdminJudgingSchedule
      summary: Get judging generation parameters and slots ordered by judge and time
      security: [{sessionCookie: []}]
      responses:
        "200": {$ref: "#/components/responses/AdminJudgingScheduleItem"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      operationId: generateAdminJudgingSchedule
      summary: Generate and atomically retain a judging schedule
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/JudgingScheduleParams"}
      responses:
        "201": {$ref: "#/components/responses/AdminJudgingScheduleItem"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/ValidationError"}
    delete:
      operationId: clearAdminJudgingSchedule
      summary: Clear all judging slots
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      responses:
        "204": {description: Judging schedule cleared}
  /admin/settings/{section}:
    parameters:
      - in: path
        name: section
        required: true
        schema: {type: string, enum: [event, integrations, network, hardware, game]}
    get:
      operationId: getAdminSettingsSection
      summary: Get one settings section with secrets represented only as configured flags
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Settings section
          content:
            application/json:
              schema:
                oneOf:
                  - {$ref: "#/components/schemas/EventSettingsResponse"}
                  - {$ref: "#/components/schemas/IntegrationSettingsResponse"}
                  - {$ref: "#/components/schemas/NetworkSettingsResponse"}
                  - {$ref: "#/components/schemas/HardwareSettingsResponse"}
                  - {$ref: "#/components/schemas/GameSettingsResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
    patch:
      operationId: updateAdminSettingsSection
      summary: Partially update one settings section
      security: [{sessionCookie: []}]
      parameters: [{$ref: "#/components/parameters/CsrfToken"}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - {$ref: "#/components/schemas/EventSettingsInput"}
                - {$ref: "#/components/schemas/IntegrationSettingsInput"}
                - {$ref: "#/components/schemas/NetworkSettingsInput"}
                - {$ref: "#/components/schemas/HardwareSettingsInput"}
                - {$ref: "#/components/schemas/GameSettingsInput"}
      responses:
        "200":
          description: Updated settings section
          content:
            application/json:
              schema:
                oneOf:
                  - {$ref: "#/components/schemas/EventSettingsResponse"}
                  - {$ref: "#/components/schemas/IntegrationSettingsResponse"}
                  - {$ref: "#/components/schemas/NetworkSettingsResponse"}
                  - {$ref: "#/components/schemas/HardwareSettingsResponse"}
                  - {$ref: "#/components/schemas/GameSettingsResponse"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/database/backups:
    post:
      operationId: createAdminDatabaseBackup
      summary: Create a server-side snapshot of the event database
      security: [{sessionCookie: []}]
      parameters:
        - {$ref: "#/components/parameters/CsrfToken"}
        - {$ref: "#/components/parameters/IdempotencyKey"}
      responses:
        "201":
          description: Backup created
          content:
            application/json:
              schema: {$ref: "#/components/schemas/DatabaseBackupResponse"}
        "409": {$ref: "#/components/responses/Conflict"}
  /admin/database/restore:
    post:
      operationId: restoreAdminDatabase
      summary: Validate and atomically restore a Cheesy Arena database backup
      description: Existing sessions are invalidated after a successful restore. Uploads are limited to 256 MiB.
      security: [{sessionCookie: []}]
      parameters:
        - {$ref: "#/components/parameters/CsrfToken"}
        - {$ref: "#/components/parameters/IdempotencyKey"}
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              additionalProperties: false
              required: [eventId, confirmation, databaseFile]
              properties:
                eventId:
                  type: integer
                  description: ID of the currently loaded event, not the uploaded event.
                confirmation: {type: string, const: restore}
                databaseFile: {type: string, format: binary}
      responses:
        "200":
          description: Database restored, or the original result when replayed
          content:
            application/json:
              schema: {$ref: "#/components/schemas/DatabaseRestoreResponse"}
        "409": {$ref: "#/components/responses/Conflict"}
        "413":
          description: Upload exceeds 256 MiB
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ErrorResponse"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/tournament-data/{type}:
    parameters:
      - in: path
        name: type
        required: true
        schema: {type: string, enum: [practice, qualification, playoff]}
    delete:
      operationId: clearAdminTournamentData
      summary: Atomically clear one tournament stage after creating a backup
      security: [{sessionCookie: []}]
      parameters:
        - {$ref: "#/components/parameters/CsrfToken"}
        - {$ref: "#/components/parameters/IdempotencyKey"}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/TournamentDataClearInput"}
      responses:
        "200":
          description: Cleared record counts, or the result of the original request when replayed
          content:
            application/json:
              schema: {$ref: "#/components/schemas/TournamentDataClearResponse"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/publishing/{resource}:
    parameters:
      - in: path
        name: resource
        required: true
        schema: {type: string, enum: [teams, matches, rankings, alliances, awards]}
    post:
      operationId: publishAdminResource
      summary: Start an idempotent background TBA publishing job
      security: [{sessionCookie: []}]
      parameters:
        - {$ref: "#/components/parameters/CsrfToken"}
        - {$ref: "#/components/parameters/IdempotencyKey"}
      responses:
        "202":
          description: Publishing job accepted
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PublishingResponse"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/ValidationError"}
  /admin/match-play/matches:
    get:
      operationId: listAdminMatchPlayMatches
      summary: Get the match selector data formerly rendered as an HTML fragment
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Match selector groups and active tab
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MatchPlayMatchesResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
  /displays/queueing/matches:
    get:
      operationId: listQueueingDisplayMatches
      summary: Get the upcoming match cards for the queueing display
      responses:
        "200":
          description: Queueing display match cards in display order
          content:
            application/json:
              schema: {$ref: "#/components/schemas/QueueingDisplayMatchesResponse"}
  /displays/announcer/match:
    get:
      operationId: getAnnouncerDisplayMatch
      summary: Get teams and rankings for the announcer display's current match
      responses:
        "200":
          description: Current match announcer data
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AnnouncerDisplayMatchResponse"}
  /displays/announcer/score:
    get:
      operationId: getAnnouncerDisplayScore
      summary: Get the most recently posted score for the announcer display
      responses:
        "200":
          description: Posted score details
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AnnouncerDisplayScoreResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: session_token
  parameters:
    ResourceId:
      in: path
      name: id
      required: true
      schema: {type: integer, minimum: 1}
    CsrfToken:
      in: header
      name: X-CSRF-Token
      required: true
      schema: {type: string}
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: true
      schema: {type: string, minLength: 1, maxLength: 128}
  responses:
    BadRequest:
      description: Malformed request
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    Unauthorized:
      description: Authentication required
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    Forbidden:
      description: Administrator permission or a valid CSRF token is required
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    ValidationError:
      description: Request failed business validation
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    Conflict:
      description: Resource state conflicts with the operation
      content:
        application/json:
          schema: {$ref: "#/components/schemas/ErrorResponse"}
    AdminTeamItem:
      description: Administrative team record
      content:
        application/json:
          schema: {$ref: "#/components/schemas/AdminTeamResponse"}
    AdminJobItem:
      description: Background job
      content:
        application/json:
          schema: {$ref: "#/components/schemas/AdminJobResponse"}
    AdminJudgingScheduleItem:
      description: Judging schedule snapshot
      content:
        application/json:
          schema: {$ref: "#/components/schemas/JudgingScheduleResponse"}
    AdminAwardList:
      description: Awards
      content:
        application/json:
          schema: {$ref: "#/components/schemas/AdminAwardListResponse"}
    AdminAwardItem:
      description: Award
      content:
        application/json:
          schema: {$ref: "#/components/schemas/AdminAwardResponse"}
    AdminLowerThirdItem:
      description: Lower third
      content:
        application/json:
          schema: {$ref: "#/components/schemas/AdminLowerThirdResponse"}
    InternalError:
      description: Unexpected server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    AdminTeam:
      type: object
      required: [id, name, nickname, city, stateProv, country, schoolName, rookieYear, robotName, accomplishments, wpaKeyConfigured, yellowCard, hasConnected, ftaNotes]
      properties:
        id: {type: integer}
        name: {type: string}
        nickname: {type: string}
        city: {type: string}
        stateProv: {type: string}
        country: {type: string}
        schoolName: {type: string}
        rookieYear: {type: integer, minimum: 0}
        robotName: {type: string}
        accomplishments: {type: string}
        wpaKeyConfigured:
          type: boolean
          description: Whether a WPA key is stored; the key itself is never returned.
        yellowCard: {type: boolean}
        hasConnected: {type: boolean}
        ftaNotes: {type: string}
    AdminTeamResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminTeam"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminTeamListData:
      type: object
      required: [teams, canModifyTeamList, networkSecurityEnabled]
      properties:
        teams: {type: array, items: {$ref: "#/components/schemas/AdminTeam"}}
        canModifyTeamList: {type: boolean}
        networkSecurityEnabled: {type: boolean}
    AdminTeamListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminTeamListData"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminTeamImportInput:
      type: object
      additionalProperties: false
      required: [teamIds, downloadOfficialData]
      properties:
        teamIds:
          type: array
          minItems: 1
          maxItems: 1000
          uniqueItems: true
          items: {type: integer, minimum: 1}
        downloadOfficialData: {type: boolean}
    SecretInput:
      type: object
      additionalProperties: false
      required: [action, value]
      properties:
        action: {type: string, enum: [keep, replace, clear]}
        value:
          type: string
          description: Required only when action is replace; ignored otherwise.
    AdminTeamUpdateInput:
      type: object
      additionalProperties: false
      required: [name, nickname, city, stateProv, country, schoolName, rookieYear, robotName, accomplishments, hasConnected, ftaNotes]
      properties:
        name: {type: string}
        nickname: {type: string}
        city: {type: string}
        stateProv: {type: string}
        country: {type: string}
        schoolName: {type: string}
        rookieYear: {type: integer, minimum: 0}
        robotName: {type: string}
        accomplishments: {type: string}
        wpaKey: {$ref: "#/components/schemas/SecretInput"}
        hasConnected: {type: boolean}
        ftaNotes: {type: string}
    AdminTeamWpaKeysInput:
      type: object
      additionalProperties: false
      required: [replaceAll]
      properties:
        replaceAll: {type: boolean}
    AdminTeamWpaKeysResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: object
          required: [updated]
          properties:
            updated: {type: integer, minimum: 0}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminJob:
      type: object
      required: [id, type, status, progress, error, createdAt, startedAt, completedAt]
      properties:
        id: {type: string, format: uuid}
        type: {type: string, enum: [team_import, team_refresh, publish_teams, publish_matches, publish_rankings, publish_alliances, publish_awards]}
        status: {type: string, enum: [queued, running, succeeded, failed]}
        progress: {type: integer, minimum: 0, maximum: 100}
        error: {type: [string, "null"]}
        createdAt: {type: string, format: date-time}
        startedAt: {type: [string, "null"], format: date-time}
        completedAt: {type: [string, "null"], format: date-time}
    AdminJobResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminJob"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    JudgingScheduleParams:
      type: object
      additionalProperties: false
      required: [numJudges, durationMinutes, previousSpacingMinutes, nextSpacingMinutes]
      properties:
        numJudges: {type: integer, minimum: 1, maximum: 100}
        durationMinutes: {type: integer, minimum: 1, maximum: 1440}
        previousSpacingMinutes: {type: integer, minimum: 1, maximum: 1440}
        nextSpacingMinutes: {type: integer, minimum: 1, maximum: 1440}
    JudgingSlot:
      type: object
      required: [id, time, teamId, previousMatchNumber, previousMatchTime, nextMatchNumber, nextMatchTime, judgeNumber]
      properties:
        id: {type: integer}
        time: {type: string, format: date-time}
        teamId: {type: integer}
        previousMatchNumber: {type: integer, minimum: 0}
        previousMatchTime:
          type: string
          description: RFC 3339, or an empty string when no previous match exists.
        nextMatchNumber: {type: integer, minimum: 0}
        nextMatchTime:
          type: string
          description: RFC 3339, or an empty string when no next match exists.
        judgeNumber: {type: integer, minimum: 1}
    JudgingSchedule:
      type: object
      required: [params, slots]
      properties:
        params: {$ref: "#/components/schemas/JudgingScheduleParams"}
        slots: {type: array, items: {$ref: "#/components/schemas/JudgingSlot"}}
    JudgingScheduleResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/JudgingSchedule"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    EventSettings:
      type: object
      required: [name, playoffType, numPlayoffAlliances, selectionRound2Order, selectionRound3Order, selectionShowUnpickedTeams, autoAudienceDisplayEnabled, adminPasswordConfigured]
      properties:
        name: {type: string}
        playoffType: {type: string, enum: [double_elimination, single_elimination]}
        numPlayoffAlliances: {type: integer, minimum: 2, maximum: 16}
        selectionRound2Order: {type: string}
        selectionRound3Order: {type: string}
        selectionShowUnpickedTeams: {type: boolean}
        autoAudienceDisplayEnabled: {type: boolean}
        adminPasswordConfigured: {type: boolean}
    EventSettingsInput:
      type: object
      additionalProperties: false
      properties:
        name: {type: string, minLength: 1}
        playoffType: {type: string, enum: [double_elimination, single_elimination]}
        numPlayoffAlliances: {type: integer, minimum: 2, maximum: 16}
        selectionRound2Order: {type: string}
        selectionRound3Order: {type: string}
        selectionShowUnpickedTeams: {type: boolean}
        autoAudienceDisplayEnabled: {type: boolean}
        adminPassword: {$ref: "#/components/schemas/SecretInput"}
    EventSettingsResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/EventSettings"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    IntegrationSettings:
      type: object
      required: [tbaDownloadEnabled, tbaPublishingEnabled, tbaEventCode, tbaSecretIdConfigured, tbaSecretConfigured, nexusEnabled, nexusAutoQueueEnabled, nexusAutoQueueKeyConfigured]
      properties:
        tbaDownloadEnabled: {type: boolean}
        tbaPublishingEnabled: {type: boolean}
        tbaEventCode: {type: string}
        tbaSecretIdConfigured: {type: boolean}
        tbaSecretConfigured: {type: boolean}
        nexusEnabled: {type: boolean}
        nexusAutoQueueEnabled: {type: boolean}
        nexusAutoQueueKeyConfigured: {type: boolean}
    IntegrationSettingsInput:
      type: object
      additionalProperties: false
      properties:
        tbaDownloadEnabled: {type: boolean}
        tbaPublishingEnabled: {type: boolean}
        tbaEventCode: {type: string}
        tbaSecretId: {$ref: "#/components/schemas/SecretInput"}
        tbaSecret: {$ref: "#/components/schemas/SecretInput"}
        nexusEnabled: {type: boolean}
        nexusAutoQueueEnabled: {type: boolean}
        nexusAutoQueueKey: {$ref: "#/components/schemas/SecretInput"}
    IntegrationSettingsResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/IntegrationSettings"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    NetworkSettings:
      type: object
      required: [networkSecurityEnabled, apAddress, apPasswordConfigured, apChannel, switchAddress, switchPasswordConfigured, sccManagementEnabled, redSccAddress, blueSccAddress, sccUsername, sccPasswordConfigured, sccUpCommands, sccDownCommands]
      properties:
        networkSecurityEnabled: {type: boolean}
        apAddress: {type: string}
        apPasswordConfigured: {type: boolean}
        apChannel: {type: integer, minimum: 1, maximum: 196}
        switchAddress: {type: string}
        switchPasswordConfigured: {type: boolean}
        sccManagementEnabled: {type: boolean}
        redSccAddress: {type: string}
        blueSccAddress: {type: string}
        sccUsername: {type: string}
        sccPasswordConfigured: {type: boolean}
        sccUpCommands: {type: string}
        sccDownCommands: {type: string}
    NetworkSettingsInput:
      type: object
      additionalProperties: false
      properties:
        networkSecurityEnabled: {type: boolean}
        apAddress: {type: string}
        apPassword: {$ref: "#/components/schemas/SecretInput"}
        apChannel: {type: integer, minimum: 1, maximum: 196}
        switchAddress: {type: string}
        switchPassword: {$ref: "#/components/schemas/SecretInput"}
        sccManagementEnabled: {type: boolean}
        redSccAddress: {type: string}
        blueSccAddress: {type: string}
        sccUsername: {type: string}
        sccPassword: {$ref: "#/components/schemas/SecretInput"}
        sccUpCommands: {type: string}
        sccDownCommands: {type: string}
    NetworkSettingsResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/NetworkSettings"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    TeamSignSettings:
      type: object
      required: [red1, red2, red3, redTimer, blue1, blue2, blue3, blueTimer]
      properties:
        red1: {type: integer, minimum: 0}
        red2: {type: integer, minimum: 0}
        red3: {type: integer, minimum: 0}
        redTimer: {type: integer, minimum: 0}
        blue1: {type: integer, minimum: 0}
        blue2: {type: integer, minimum: 0}
        blue3: {type: integer, minimum: 0}
        blueTimer: {type: integer, minimum: 0}
    TeamSignSettingsInput:
      type: object
      additionalProperties: false
      properties:
        red1: {type: integer, minimum: 0}
        red2: {type: integer, minimum: 0}
        red3: {type: integer, minimum: 0}
        redTimer: {type: integer, minimum: 0}
        blue1: {type: integer, minimum: 0}
        blue2: {type: integer, minimum: 0}
        blue3: {type: integer, minimum: 0}
        blueTimer: {type: integer, minimum: 0}
    CompanionButton:
      type: object
      required: [page, row, column]
      properties:
        page: {type: integer, minimum: 0}
        row: {type: integer, minimum: 0}
        column: {type: integer, minimum: 0}
    CompanionButtonInput:
      type: object
      additionalProperties: false
      properties:
        page: {type: integer, minimum: 0}
        row: {type: integer, minimum: 0}
        column: {type: integer, minimum: 0}
    CompanionSettings:
      type: object
      required: [address, port, matchPreview, setAudience, matchStart, teleopStart, endgameStart, matchEnd, postResult, allianceSelection, matchAbort]
      properties:
        address: {type: string}
        port: {type: integer, minimum: 0, maximum: 65535}
        matchPreview: {$ref: "#/components/schemas/CompanionButton"}
        setAudience: {$ref: "#/components/schemas/CompanionButton"}
        matchStart: {$ref: "#/components/schemas/CompanionButton"}
        teleopStart: {$ref: "#/components/schemas/CompanionButton"}
        endgameStart: {$ref: "#/components/schemas/CompanionButton"}
        matchEnd: {$ref: "#/components/schemas/CompanionButton"}
        postResult: {$ref: "#/components/schemas/CompanionButton"}
        allianceSelection: {$ref: "#/components/schemas/CompanionButton"}
        matchAbort: {$ref: "#/components/schemas/CompanionButton"}
    CompanionSettingsInput:
      type: object
      additionalProperties: false
      properties:
        address: {type: string}
        port: {type: integer, minimum: 0, maximum: 65535}
        matchPreview: {$ref: "#/components/schemas/CompanionButtonInput"}
        setAudience: {$ref: "#/components/schemas/CompanionButtonInput"}
        matchStart: {$ref: "#/components/schemas/CompanionButtonInput"}
        teleopStart: {$ref: "#/components/schemas/CompanionButtonInput"}
        endgameStart: {$ref: "#/components/schemas/CompanionButtonInput"}
        matchEnd: {$ref: "#/components/schemas/CompanionButtonInput"}
        postResult: {$ref: "#/components/schemas/CompanionButtonInput"}
        allianceSelection: {$ref: "#/components/schemas/CompanionButtonInput"}
        matchAbort: {$ref: "#/components/schemas/CompanionButtonInput"}
    HardwareSettings:
      type: object
      required: [plcAddress, ledControllerAddress, ledUniverseMode, teamSigns, useLiteUdpPort, blackmagicAddresses, companion]
      properties:
        plcAddress: {type: string}
        ledControllerAddress: {type: string}
        ledUniverseMode: {type: string, enum: [single, two]}
        teamSigns: {$ref: "#/components/schemas/TeamSignSettings"}
        useLiteUdpPort: {type: boolean}
        blackmagicAddresses: {type: string}
        companion: {$ref: "#/components/schemas/CompanionSettings"}
    HardwareSettingsInput:
      type: object
      additionalProperties: false
      properties:
        plcAddress: {type: string}
        ledControllerAddress: {type: string}
        ledUniverseMode: {type: string, enum: [single, two]}
        teamSigns: {$ref: "#/components/schemas/TeamSignSettingsInput"}
        useLiteUdpPort: {type: boolean}
        blackmagicAddresses: {type: string}
        companion: {$ref: "#/components/schemas/CompanionSettingsInput"}
    HardwareSettingsResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/HardwareSettings"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    DatabaseBackupResult:
      type: object
      required: [created, replayed]
      properties:
        created: {type: boolean}
        replayed: {type: boolean}
    DatabaseBackupResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/DatabaseBackupResult"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    DatabaseRestoreResult:
      type: object
      required: [restored, replayed]
      properties:
        restored: {type: boolean}
        replayed: {type: boolean}
    DatabaseRestoreResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/DatabaseRestoreResult"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    TournamentDataClearInput:
      type: object
      additionalProperties: false
      required: [eventId, confirmation]
      properties:
        eventId: {type: integer, minimum: 1}
        confirmation:
          type: string
          description: Must exactly equal clear:{type}, for example clear:qualification.
    TournamentDataClearResult:
      type: object
      required: [matchType, matches, matchResults, scheduledBreaks, rankings, alliances, replayed]
      properties:
        matchType: {type: string, enum: [practice, qualification, playoff]}
        matches: {type: integer, minimum: 0}
        matchResults: {type: integer, minimum: 0}
        scheduledBreaks: {type: integer, minimum: 0}
        rankings: {type: integer, minimum: 0}
        alliances: {type: integer, minimum: 0}
        replayed: {type: boolean}
    TournamentDataClearResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/TournamentDataClearResult"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    PublishingResult:
      type: object
      required: [job, replayed]
      properties:
        job: {$ref: "#/components/schemas/AdminJob"}
        replayed: {type: boolean}
    PublishingResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/PublishingResult"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    MatchPlayListItem:
      type: object
      required: [id, shortName, scheduledAt, displayTime, status, colorClass, canShowResult]
      properties:
        id: {type: integer}
        shortName: {type: string}
        scheduledAt:
          type: string
          description: RFC 3339, or empty when the match has no scheduled time.
        displayTime: {type: string, description: Localized legacy display string.}
        status: {$ref: "#/components/schemas/MatchStatus"}
        colorClass: {type: string, enum: ["", red, blue, yellow, green]}
        canShowResult: {type: boolean}
    MatchPlayMatches:
      type: object
      required: [currentMatchType, matchesByType]
      properties:
        currentMatchType: {type: string, enum: [practice, qualification, playoff]}
        matchesByType:
          type: object
          required: [practice, qualification, playoff]
          properties:
            practice: {type: array, items: {$ref: "#/components/schemas/MatchPlayListItem"}}
            qualification: {type: array, items: {$ref: "#/components/schemas/MatchPlayListItem"}}
            playoff: {type: array, items: {$ref: "#/components/schemas/MatchPlayListItem"}}
    MatchPlayMatchesResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/MatchPlayMatches"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    QueueingDisplayAlliance:
      type: object
      required: [teamIds, offFieldTeamIds, playoffAllianceId]
      properties:
        teamIds: {type: array, items: {type: integer}}
        offFieldTeamIds: {type: array, items: {type: integer}}
        playoffAllianceId: {type: integer, minimum: 0}
    QueueingDisplayMatch:
      type: object
      required: [position, positionLabel, id, shortName, scheduledAt, displayTime, red, blue]
      properties:
        position: {type: string, pattern: "^(on_field|on_deck|up_in_[0-9]+)$"}
        positionLabel: {type: string}
        id: {type: integer}
        shortName: {type: string}
        scheduledAt:
          type: string
          description: RFC 3339, or empty when the match has no scheduled time.
        displayTime: {type: string, description: Localized legacy display string.}
        red: {$ref: "#/components/schemas/QueueingDisplayAlliance"}
        blue: {$ref: "#/components/schemas/QueueingDisplayAlliance"}
    QueueingDisplayMatchesResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/QueueingDisplayMatch"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AnnouncerDisplayTeam:
      type: object
      required: [id, nickname, schoolName, city, stateProv, country, rookieYear, robotName, accomplishments, rank, isOffField]
      properties:
        id: {type: integer}
        nickname: {type: string}
        schoolName: {type: string}
        city: {type: string}
        stateProv: {type: string}
        country: {type: string}
        rookieYear: {type: integer}
        robotName: {type: string}
        accomplishments: {type: string}
        rank: {type: [integer, "null"]}
        isOffField: {type: boolean}
    AnnouncerDisplayAlliance:
      type: object
      required: [playoffAllianceId, teams]
      properties:
        playoffAllianceId: {type: integer, minimum: 0}
        teams:
          type: array
          items:
            anyOf:
              - {$ref: "#/components/schemas/AnnouncerDisplayTeam"}
              - {type: "null"}
    AnnouncerDisplayMatch:
      type: object
      required: [id, type, longName, red, blue]
      properties:
        id: {type: integer}
        type: {type: string, enum: [test, practice, qualification, playoff]}
        longName: {type: string}
        red: {$ref: "#/components/schemas/AnnouncerDisplayAlliance"}
        blue: {$ref: "#/components/schemas/AnnouncerDisplayAlliance"}
    AnnouncerDisplayMatchResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AnnouncerDisplayMatch"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AnnouncerDisplayScoreResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: object
          description: Posted match metadata, winner styling, and red/blue score summaries, fouls, cards, and rankings.
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    GameSettings:
      type: object
      required: [autoDurationSec, pauseDurationSec, transitionShiftDurationSec, shiftDurationSec, endgameDurationSec, energizedBonusThreshold, superchargedBonusThreshold, traversalBonusThreshold]
      properties:
        autoDurationSec: {type: integer, minimum: 0, maximum: 86400}
        pauseDurationSec: {type: integer, minimum: 0, maximum: 86400}
        transitionShiftDurationSec: {type: integer, minimum: 0, maximum: 86400}
        shiftDurationSec: {type: integer, minimum: 0, maximum: 86400}
        endgameDurationSec: {type: integer, minimum: 0, maximum: 86400}
        energizedBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
        superchargedBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
        traversalBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
    GameSettingsInput:
      type: object
      additionalProperties: false
      properties:
        autoDurationSec: {type: integer, minimum: 0, maximum: 86400}
        pauseDurationSec: {type: integer, minimum: 0, maximum: 86400}
        transitionShiftDurationSec: {type: integer, minimum: 0, maximum: 86400}
        shiftDurationSec: {type: integer, minimum: 0, maximum: 86400}
        endgameDurationSec: {type: integer, minimum: 0, maximum: 86400}
        energizedBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
        superchargedBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
        traversalBonusThreshold: {type: integer, minimum: 0, maximum: 86400}
    GameSettingsResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/GameSettings"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminAwardInput:
      type: object
      additionalProperties: false
      required: [awardName, teamId, personName]
      properties:
        awardName: {type: string, minLength: 1}
        teamId: {type: integer}
        personName: {type: string}
    AdminAward:
      allOf:
        - {$ref: "#/components/schemas/AdminAwardInput"}
        - type: object
          required: [id]
          properties:
            id: {type: integer}
    AdminAwardResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminAward"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminAwardListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/AdminAward"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminScheduledBreakInput:
      type: object
      additionalProperties: false
      required: [description]
      properties:
        description: {type: string}
    AdminScheduledBreak:
      type: object
      required: [id, matchType, typeOrderBefore, time, durationSec, description]
      properties:
        id: {type: integer}
        matchType: {$ref: "#/components/schemas/MatchType"}
        typeOrderBefore: {type: integer}
        time: {type: string, format: date-time}
        durationSec: {type: integer, minimum: 0}
        description: {type: string}
    AdminScheduledBreakResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminScheduledBreak"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminScheduledBreakListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/AdminScheduledBreak"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminSponsorSlideInput:
      type: object
      additionalProperties: false
      required: [subtitle, line1, line2, image, displayTimeSec]
      properties:
        subtitle: {type: string}
        line1: {type: string}
        line2: {type: string}
        image: {type: string}
        displayTimeSec: {type: integer, minimum: 1}
    SponsorSlideResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/SponsorSlide"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminLowerThirdInput:
      type: object
      additionalProperties: false
      required: [topText, bottomText]
      properties:
        topText: {type: string, minLength: 1}
        bottomText: {type: string}
    AdminLowerThird:
      allOf:
        - {$ref: "#/components/schemas/AdminLowerThirdInput"}
        - type: object
          required: [id, displayOrder, awardId]
          properties:
            id: {type: integer}
            displayOrder: {type: integer}
            awardId: {type: integer}
    AdminLowerThirdResponse:
      type: object
      required: [data, meta]
      properties:
        data: {$ref: "#/components/schemas/AdminLowerThird"}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminLowerThirdListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/AdminLowerThird"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    AdminReorderInput:
      type: object
      additionalProperties: false
      required: [ids]
      properties:
        ids: {type: array, items: {type: integer}}
    MatchType:
      type: string
      enum: [test, practice, qualification, playoff]
    MatchStatus:
      type: string
      enum: [scheduled, hidden, red_won, blue_won, tie]
    ResponseMeta:
      type: object
      required:
        - requestId
      properties:
        requestId:
          type: string
        version:
          type: integer
          minimum: 0
    ErrorDetail:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
        fields:
          type: object
          additionalProperties:
            type: string
    ErrorResponse:
      type: object
      required:
        - error
        - meta
      properties:
        error:
          $ref: "#/components/schemas/ErrorDetail"
        meta:
          $ref: "#/components/schemas/ResponseMeta"
    MatchTiming:
      type: object
      required:
        - autoDurationSec
        - pauseDurationSec
        - transitionShiftDurationSec
        - shiftDurationSec
        - endgameDurationSec
      properties:
        autoDurationSec:
          type: integer
          minimum: 0
        pauseDurationSec:
          type: integer
          minimum: 0
        transitionShiftDurationSec:
          type: integer
          minimum: 0
        shiftDurationSec:
          type: integer
          minimum: 0
        endgameDurationSec:
          type: integer
          minimum: 0
    PublicEvent:
      type: object
      required:
        - id
        - name
        - playoffType
        - numPlayoffAlliances
        - selectionShowUnpickedTeams
        - networkSecurityEnabled
        - matchTiming
      properties:
        id:
          type: integer
        name:
          type: string
        playoffType:
          type: string
          enum:
            - double_elimination
            - single_elimination
        numPlayoffAlliances:
          type: integer
          minimum: 2
          maximum: 16
        selectionShowUnpickedTeams:
          type: boolean
        networkSecurityEnabled:
          type: boolean
        matchTiming:
          $ref: "#/components/schemas/MatchTiming"
    PublicEventResponse:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          $ref: "#/components/schemas/PublicEvent"
        meta:
          $ref: "#/components/schemas/ResponseMeta"
    Session:
      type: object
      required:
        - authenticated
        - admin
        - csrfToken
      properties:
        authenticated:
          type: boolean
        admin:
          type: boolean
        username:
          type:
            - string
            - "null"
        csrfToken:
          type: string
    SessionResponse:
      type: object
      required:
        - data
        - meta
      properties:
        data:
          $ref: "#/components/schemas/Session"
        meta:
          $ref: "#/components/schemas/ResponseMeta"
    MatchTeam:
      type: object
      required: [teamId, isSurrogate]
      properties:
        teamId:
          type: integer
        isSurrogate:
          type: boolean
    Match:
      type: object
      required: [id, type, typeOrder, time, longName, shortName, nameDetail, playoffMatchGroupId, playoffRedAlliance, playoffBlueAlliance, red, blue, startedAt, scoreCommittedAt, fieldReadyAt, status, useTiebreakCriteria, tbaMatchKey]
      properties:
        id: {type: integer}
        type: {$ref: "#/components/schemas/MatchType"}
        typeOrder: {type: integer}
        time: {type: string, format: date-time}
        longName: {type: string}
        shortName: {type: string}
        nameDetail: {type: string}
        playoffMatchGroupId: {type: string}
        playoffRedAlliance: {type: integer}
        playoffBlueAlliance: {type: integer}
        red:
          type: array
          minItems: 3
          maxItems: 3
          items: {$ref: "#/components/schemas/MatchTeam"}
        blue:
          type: array
          minItems: 3
          maxItems: 3
          items: {$ref: "#/components/schemas/MatchTeam"}
        startedAt: {type: [string, "null"], format: date-time}
        scoreCommittedAt: {type: [string, "null"], format: date-time}
        fieldReadyAt: {type: [string, "null"], format: date-time}
        status: {$ref: "#/components/schemas/MatchStatus"}
        useTiebreakCriteria: {type: boolean}
        tbaMatchKey:
          type: object
          required: [compLevel, setNumber, matchNumber]
          properties:
            compLevel: {type: string}
            setNumber: {type: integer}
            matchNumber: {type: integer}
    ScoreSummary:
      type: object
      additionalProperties: false
      required: [score, matchPoints, postMatchPoints, foulPoints, bonusRankingPoints, playoffDq]
      properties:
        autoFuelPoints: {type: integer}
        autoTowerPoints: {type: integer}
        teleopFuelPoints: {type: integer}
        teleopTowerPoints: {type: integer}
        numFuel: {type: integer}
        numFuelPostMatch: {type: integer}
        numFuelGoal: {type: integer}
        matchPoints: {type: integer}
        postMatchPoints: {type: integer}
        foulPoints: {type: integer}
        score: {type: integer}
        playoffDq: {type: boolean}
        energizedBonusRankingPoint: {type: boolean}
        superchargedBonusRankingPoint: {type: boolean}
        traversalBonusRankingPoint: {type: boolean}
        bonusRankingPoints: {type: integer}
        numOpponentMajorFouls: {type: integer}
    MatchResult:
      type: object
      required: [id, playNumber, redSummary, blueSummary, redCards, blueCards]
      properties:
        id: {type: integer}
        playNumber: {type: integer}
        redSummary: {$ref: "#/components/schemas/ScoreSummary"}
        blueSummary: {$ref: "#/components/schemas/ScoreSummary"}
        redCards: {type: object, additionalProperties: {type: string}}
        blueCards: {type: object, additionalProperties: {type: string}}
    MatchWithResult:
      type: object
      required: [match, result]
      properties:
        match: {$ref: "#/components/schemas/Match"}
        result:
          oneOf:
            - {$ref: "#/components/schemas/MatchResult"}
            - {type: "null"}
    MatchListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/MatchWithResult"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    Alliance:
      type: object
      required: [id, teamIds, lineup]
      properties:
        id: {type: integer}
        teamIds: {type: array, items: {type: integer}}
        lineup: {type: array, minItems: 3, maxItems: 3, items: {type: integer}}
    AllianceListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/Alliance"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    Ranking:
      type: object
      required: [teamId, nickname, rank, previousRank, rankingPoints, matchPoints, autoFuelPoints, towerPoints, wins, losses, ties, disqualifications, played]
      properties:
        teamId: {type: integer}
        nickname: {type: string}
        rank: {type: integer}
        previousRank: {type: integer}
        rankingPoints: {type: integer}
        matchPoints: {type: integer}
        autoFuelPoints: {type: integer}
        towerPoints: {type: integer}
        wins: {type: integer}
        losses: {type: integer}
        ties: {type: integer}
        disqualifications: {type: integer}
        played: {type: integer}
    RankingsResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: object
          required: [rankings, highestPlayedMatch]
          properties:
            rankings: {type: array, items: {$ref: "#/components/schemas/Ranking"}}
            highestPlayedMatch: {type: string}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    SponsorSlide:
      type: object
      required: [id, subtitle, line1, line2, image, displayTimeSec, displayOrder]
      properties:
        id: {type: integer}
        subtitle: {type: string}
        line1: {type: string}
        line2: {type: string}
        image: {type: string}
        displayTimeSec: {type: integer}
        displayOrder: {type: integer}
    SponsorSlideListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/SponsorSlide"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    PublicTeam:
      type: object
      required: [id, name, nickname, city, stateProv, country, schoolName, rookieYear, robotName, accomplishments, yellowCard, hasConnected]
      properties:
        id: {type: integer}
        name: {type: string}
        nickname: {type: string}
        city: {type: string}
        stateProv: {type: string}
        country: {type: string}
        schoolName: {type: string}
        rookieYear: {type: integer}
        robotName: {type: string}
        accomplishments: {type: string}
        yellowCard: {type: boolean}
        hasConnected: {type: boolean}
    TeamListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/PublicTeam"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    Rule:
      type: object
      required: [id, ruleNumber, isMajor, isRankingPoint, description]
      properties:
        id: {type: integer}
        ruleNumber: {type: string}
        isMajor: {type: boolean}
        isRankingPoint: {type: boolean}
        description: {type: string}
    RuleListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: array, items: {$ref: "#/components/schemas/Rule"}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    BracketResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: object
          required: [bracketType, activeMatchId, matchups]
          properties:
            bracketType: {type: string, enum: [double, double4, "16", "8", "4", "2"]}
            activeMatchId: {type: [integer, "null"]}
            matchups: {type: array, items: {type: object}}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    MatchLogListResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: object}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
    MatchLogDetailResponse:
      type: object
      required: [data, meta]
      properties:
        data: {type: object}
        meta: {$ref: "#/components/schemas/ResponseMeta"}
